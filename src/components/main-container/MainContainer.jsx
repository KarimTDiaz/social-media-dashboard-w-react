import Header from '../header/Header';
import { StyledMainContainer } from './styles';

const MainContainer = () => {
	return (
		<StyledMainContainer>
			<Header></Header>
		</StyledMainContainer>
	);
};

export default MainContainer;
